# Human TODO

- Collect 15–20 real award letters from teammates/friends; redact; keep in
  untracked `real-letters/`; run `npm run eval` against them locally.
- Register + verify rules/eligibility for each hackathon (especially
  cross-submission clauses and Reverie/Iris build windows).
- Vercel deploy + env var; record 3-minute demo video from `DEMO_SCRIPT.md`;
  finalize + submit Devpost entries; pick final product name.
